# Разбор аналитики odin-dva.ru — 2026-09-10

Сгенерирован `scripts/seo/analyze.py` по последним выгрузкам в `docs/analytics/data/`. Это чтение цифр, а не список задач: решать, что из этого делать, всё равно тебе.

## Поиск Google — общее

- Период выгрузки: `2026-09-07_90d`
- **0 кликов**, 0 показов, средний CTR **0.00%**
- ⚠ Постранично GSC не отдал ни строки, хотя по дням набирается 1 кликов и 5 показов. Это порог приватности: разбивку Google скрывает, пока цифры маленькие. Итог за период смотреть в `TREND.md`, разделы ниже на таких данных не считаются.

## Данных мало

- За период 0 показов при пороге в 200. Разделы про CTR, каннибализацию и запросы у порога топа ниже ничего не покажут — не потому что всё хорошо, а потому что считать не из чего.
- Пока сайт на таких объёмах, единственные осмысленные цифры — индексация (`--inspect`) и доступность самих страниц.

## Динамика внутри периода

- Первая половина 1 кликов → вторая 0 (-100%), падает.
- Внутри одного месяца это ещё не тренд, но если знак повторится в следующем замере — уже сигнал.

## Мобильные и десктоп

- Мобильных кликов 0 из 0 — **0%** всего трафика, CTR 0.00% против 25.00% на десктопе.
- Позиция: 10.0 мобильная, 1.0 десктопная (разрыв -9.0).
- Разрыв больше двух позиций — повод проверить скорость и вёрстку на телефоне.

## Счётчики: поведение и сверка

- Метрика: **9 визитов**, 9 посетителей, отказы 66.7%.
- Из них **1 визитов — internal traffic**, свои же заходы. Их надо вычитать из любых выводов о трафике.
- Страницы входа с отказами выше 45% — заходят и сразу уходят:
  - `/` — 9 визитов, отказы 67%
- GA4: 6 сеансов (для сверки со счётчиком Яндекса).
- Счётчики расходятся на 33% — многовато, стоит посмотреть, не отваливается ли один из них на части страниц.
